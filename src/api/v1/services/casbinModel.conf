[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = r.sub == 'role:super-admin' || r.sub == 'role:1' || (r.sub == p.sub && (p.obj == '*' || r.obj == p.obj) && (p.act == '*' || r.act == p.act))